name: Populate Release Notes Template

on:
  push:
    branches:
      - master

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  release-notes:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Fetch Commit History
      run: |
        git log --pretty=format:'%s' [LATEST COMMIT]..HEAD > commits.txt

    - name: Parse Commit Messages
      run: |
        # parse the commit messages and extract information about new features, bug fixes, and improvements

    - name: Populate Template
      run: |
        # populate the release note template with the extracted information

    - name: Create Release
      uses: actions/create-release@v1
      with:
        tag_name: [VERSION NUMBER]
        release_name: [RELEASE NAME]
        draft: false
        prerelease: false
        body_path: [RELEASE NOTES FILE PATH]
