name: Create Release Notes

on:
  push:
    branches:
      - main

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  release-notes:
    runs-on: ubuntu-latest

    steps:
    - name: Fetch Commit History
      run: |
        git clone https://github.com/${{ github.repository }}.git
        cd ${{ github.repository }}
        git checkout ${{ github.ref }}

    - name: Parse Commit Messages
      run: |
        # parse commit messages and extract information about new features, bug fixes, and improvements

    - name: Populate Template
      run: |
        # populate the template with the information extracted from the commit history

    - name: Publish Release Notes
      uses: actions/create-release@v1
      with:
        tag_name: [VERSION NUMBER]
        release_name: [RELEASE NAME]
        draft: false
        prerelease: false
        body_path: [RELEASE NOTES FILE PATH]
